# eks-far-2-cel-demo-old

## Important: Terraform on AWS CloudShell

AWS CloudShell has limited disk space.

To avoid provider installation failures:
```bash
export TF_PLUGIN_CACHE_DIR=/tmp/terraform-plugin-cache
